using NUnit.Framework;

namespace OpenDeploymentManager.Server.IntegrationTests
{
    [TestFixture]
    public class OpenDeploymentManagerConnectionFixture
    {
        ////[Test]
        ////public void Test()
        ////{
        ////    // arrange
        ////    var clientFactory = new OpenDeploymentManagerClientFactory();
            
        ////    var endpoint = new OpenDeploymentManagerEndpoint(
        ////        new Uri(ServerConfiguration.ServerUrl),
        ////        new BearerTokenAuthentication("admin", "123456"));

        ////    IOpenDeploymentManagerClient client = clientFactory.CreateClient(endpoint);

        ////    // act

        ////    // assert
        ////}
    }
}
